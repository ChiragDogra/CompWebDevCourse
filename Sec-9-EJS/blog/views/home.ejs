<%- include('partials/header'); -%>

<p><%=stContent%></p>

<div>
  <% allPosts.forEach((post)=>{ %>
    <% let lnk = "/posts/" + post.title %>
  <div>
    <h1><%= post.title %></h1>
    <% if(post.text.length < 100){ %>
    <p><%= post.text %> <a href= "/posts/<%= post.title %>" >read more</a></p>
    <% }else{ %> <% let str = post.text.slice(0, 100) + "..." %>
    <p><%= str %><a href= "/posts/<%= post.title %>" >read more</a></p>
    <% } %>
  </div>
  <% }) %>
</div>

<%- include('partials/footer'); -%>
